# 요구사항정의서 → 요청문 매핑 규칙

`요구사항정의서_엑셀양식_v3_1_3.xlsx` 1행 = 요청문 1건. 12개 컬럼을 7줄로 줄이는 규칙.

---

## 1. 매핑표

| 엑셀 컬럼 | 요청문 필드 | 처리 |
|---|---|---|
| 요구사항명 + 상세내용 `[개요]` | `목표` | 병합 |
| 상세내용 `[검증 기준]` | `목표` | 흡수 |
| 상세내용 `[제약 조건]` | `제약` | 이동 |
| 진행 현황 | `성격` | 치환 |
| 대·중·소분류 | `경로` | 판단 근거 |
| 담당자 (DEV/SE) | `워커` | 치환 |
| 요구사항 ID | (프리픽스) | 다건 처리 시에만 |
| 요청자 · 우선순위 · 수용 여부 · 비고 | — | 생략 |

---

## 2. 컬럼별 근거

### 요구사항명 + `[개요]` → 목표

요구사항명은 명사구(`다중 클러스터 설정 정보 수집`)라 그대로 쓰면 완료 조건이 안 된다. `[개요]`의 서술을 붙여 "무엇이 되어 있으면 끝인지"로 만든다.

### `[검증 기준]` → 목표에 흡수 (별도 섹션 만들지 않음)

Verification 검증은 오케스트레이터가 수행한다. 사용자는 판정 기준만 목표 안에 넣는다. `완료 조건 = ① ② ③` 형태로 붙인다.

> 별도 `검증:` 줄을 만들면 오케스트레이터가 생성하는 task.md 검증 항목과 이중화되어, 어느 쪽이 정본인지 모호해진다.

### `[제약 조건 / 비기능 요구사항]` → 제약

**Do NOT만 남긴다.** `[제약 조건]`은 보통 금지(쓰기 금지)와 준수(TLS 적용, 페이징 처리)가 섞여 있는데, 준수 항목은 목표의 완료 조건이거나 전제이므로 `제약`에 넣으면 긍정문 사이에서 무시된다.

- 금지 → `제약`
- 달성해야 할 품질 → `목표`
- 이미 정해진 환경 → 전제로 한 줄만 (`~를 전제로`)

### 진행 현황 → 성격

`성격`이 워커 선택과 `target_repo` 질문 여부를 결정하므로 반드시 채워야 한다. 진행 현황이 그 값을 그대로 알려준다.

| 진행 현황 | 성격 | 실제 산출물 |
|---|---|---|
| 기획중 | 분석·요약 | 설계 문서 |
| 설계중 | 분석·요약 | 상세 설계·스키마 |
| 개발중 | 코드 구현 | diff / 소스 |
| 검증중 | 코드 구현 | 테스트 케이스 |

### 대·중·소분류 → 경로 판단 근거

`Discovery > Docker > K8S`는 요청문에 직접 쓰지 않는다. 어느 모듈·레포인지 특정하는 데만 쓰고, 결과값인 `target_repo` 절대경로만 요청문에 남긴다.

### 담당자 → 워커

`DEV: 이민규 / SE: 홍성훈`은 사람 이름이라 AI에 의미가 없다. 직무 구성을 워커 조합으로 치환한다.

- DEV 단독 → `생산=claude-main`
- DEV + SE → `생산=claude-main, 리뷰=codex-critic` (운영 관점 검토가 필요하다는 신호)
- 이미지·대용량 문서 포함 → 리뷰를 `gemini`로

### 생략 컬럼

| 컬럼 | 생략 이유 |
|---|---|
| 요청자 | 산출물 내용에 영향 없음 |
| 우선순위 | 요청문 1건 = 1요구사항이므로 무의미 |
| 수용 여부 | `수용`이 아닌 행은 요청문으로 만들지 않음 |
| 비고 | 미확정 사항이면 목표에서 범위를 좁혀 표현 |

> 다건을 한 세션에서 순차 처리할 때만 `[REQ-F-001|High]`를 목표 줄 앞에 프리픽스로 붙인다. 줄 수는 그대로 유지된다.

---

## 3. 변환 예시

### REQ-F-001 (기획중)

```text
목표:        등록된 N개 K8s 클러스터의 설정·상태 정보를 cluster ID별로 분리
             수집·저장하는 Discovery 모듈 설계. 완료 조건 =
             ① 복수 클러스터 등록 시 cluster ID별 분리 수집·조회
             ② 특정 클러스터 접속 실패 시 해당 건만 실패 표시, 나머지 정상 완료
             ③ 증분 수집 후 변경 리소스 최신 상태가 이력과 함께 반영
성격:        분석·요약
target_repo: {HyperMig Discovery 절대경로}
write_scope: tasks-only
워커:        생산=claude-main, 리뷰=codex-critic
산출물:      설계 문서로 artifacts/에
제약:        대상 클러스터 쓰기 작업 금지, read-only 최소 권한·rate limit 전제,
             인증정보 at-rest 암호화·전송 TLS, 클러스터 단위 장애 격리 유지
```

원본 F2 셀의 `[상세 기능 요구사항] 1)~4)`는 요청문에 옮기지 않았다. 목표와 제약이 범위를 규정하면 오케스트레이터가 brief.md에서 세분화한다.

### REQ-F-002 (개발중)

```text
목표:        Helm values, Kustomize overlay, ApplicationSet generator로 분기된
             dev/stg/prod 환경별 설정 파라미터를 수집·병합. 완료 조건 =
             동일 리소스에 대해 환경별 최종 병합값이 대상 클러스터 실제 적용값과 일치
성격:        코드 구현
target_repo: {HyperMig Discovery 절대경로}
write_scope: none(diff-first)
워커:        생산=claude-main, 리뷰=codex-critic
산출물:      diff로 artifacts/에
제약:        기존 Docker 수집기 인터페이스 수정금지, 직접 커밋 금지
```

---

## 4. 변환 시 채워야 하는 공백

엑셀에 없어서 사람이 판단해야 하는 값.

| 필드 | 왜 없는가 | 처리 |
|---|---|---|
| `target_repo` | 시트에 레포 경로 컬럼이 없음 | 절대경로 직접 입력. 모듈이 같으면 재사용 |
| `write_scope` | 시트에 없음 | 기본 `none(diff-first)`. 분석 요청은 `tasks-only` |
| `산출물` | 시트에 없음 | 진행 현황에서 유도 (표 참조) |
| `[검증 기준]` 누락 행 | 상세 내용을 한 문단으로만 작성한 행 | 완료 조건을 직접 작성 |

REQ-F-002가 후자에 해당한다. 근본 해결은 원본 시트 `상세 내용`을 REQ-F-001처럼 `[개요] / [상세 기능 요구사항] / [입력·출력] / [제약 조건] / [검증 기준]` 5블록으로 작성하는 것이다. 이 구조가 지켜진 행은 기계적으로 변환된다.

---

## 5. 요약

1. 목표에 완료 조건까지 넣는다 (검증 섹션 만들지 않음)
2. 제약은 Do NOT만
3. 진행 현황이 성격과 산출물을 결정한다
4. 담당자는 워커로, 분류는 경로로 치환
5. 요청자·우선순위·수용 여부·비고는 버린다
